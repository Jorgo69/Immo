# ============================================================
# Exemple de variables d'environnement (copier en .env si besoin)
# Les valeurs par défaut sont dans src/config/default.yml
# ============================================================

# Canaux d'envoi OTP : email, whatsapp, sms (sms = Twilio, désactivé par défaut)
# AUTH_CHANNELS=email,whatsapp

# SMTP (Nodemailer) — pour envoi du code par email
# SMTP_HOST=localhost
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=Immo Bénin <noreply@immo-benin.local>

# Twilio (optionnel, pour canal SMS)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=

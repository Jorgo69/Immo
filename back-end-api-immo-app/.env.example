# ============================================================
# Variables d'environnement (optionnel)
# ============================================================
# La configuration principale se fait via src/config/default.yml
# (copier default.yml.example en default.yml).
#
# En production, vous pouvez ignorer le fichier YAML et tout
# définir ici. Noms reconnus : voir les commentaires en tête
# de src/config/default.yml.example (DATABASE_*, JWT_SECRET,
# ENCRYPTION_KEY, SMTP_*, etc.).
# ============================================================

# En dev : utiliser /api pour que le proxy n’intercepte pas les routes SPA (/property, /auth…).
# Sans cela, un rechargement sur /property renverrait du JSON (proxy vers le backend).
VITE_API_BASE_URL=/api

# Exemple prod :
# VITE_API_BASE_URL=https://api.immo-benin.com

